以下按“完全未实施 / 部分实施 / 需外部条件才能生效”三类，如实列出当前仍未落地的建议。

完全未实施

| 建议 | 来源 | 说明 |
| :-- | :-- | :-- |
| **Step 级完整 Trace**（决策链每一步记录 input/output/rule_id/changed/reason 并落库） | 最近一轮 P0 | 目前只有 `context.trace` 摘要，未做逐步骤全量审计记录 |
| **Decision-level OOS / Regime OOS / Stock-level OOS**（Permission/Setup/FSM/Exit 分层 OOS，Bull/Bear/Sideways/Recovery/Crash 分域、单股贡献剔除） | 多轮 P1 | 未实现，现有 OOS 只有组合级 Rolling OOS |
| **Event-level OOS（波段事件级）**：Entry Delay / Peak Capture / False Entry / Missed Wave 的统计检验 | 多轮 P1 | Wave Capture 指标已有，但无事件级 OOS 显著性检验 |
| **统一 Ablation Framework / Ablation Ledger**（Experiment Definition → Model Variant → 同 Dataset/PIT/Cost → 统计检验 → Ablation Ledger 表） | 最近一轮 P1 | 仍是脚本 + JSON，没有统一的实验框架与台账表 |
| **PIT Truncation Replay（全历史）**：删除 T 之后数据 → 决策与全量完全一致，全股票×全历史 mismatch=0 | 早期 P1 | 只有 as-of 单元测试与重放确定性测试，未做全历史截断重放验收 |
| **Report Equality 全历史**：所有历史决策 Report==Ledger==Backtest==Shadow 100% 一致 | 早期 P1 | 只有单样本单元测试 |
| **G0–G9 单一研究门**：把老 10 门 run_status 与新 G0–G7 合并成一套 | 最近一轮 P1 | 两套体系并存（run_status 10 门 + research_gate G0–G7） |
| **报告“每个箭头可点击/追踪”**（交互式决策链） | 最近一轮 P1 | 静态文本，无交互 |
| **Slippage / Gap 压力测试**（跳空成本、隔夜跳空冲击） | 早期 P2 | 成本倍数压力已做，跳空专项未做 |
| **daily_alpha_test / layer_ablation 切换 canonical** | 最近一轮 P0 | 只改了 cross_sectional_backtest，这两个工具仍用旧 target |

部分实施（有雏形，未达审查要求）

| 建议 | 现状 |
| :-- | :-- |
| **Governance Decision Card 顶部身份信息** | 卡上显示了 Ledger/run_id、权限、TQS 等，但 **Decision ID / Settings Hash / Feature Manifest / PIT Grade / Evidence Grade / Decision Status** 未全部放上卡片（审计章节才有） |
| **Ablation 机制报告** | 已输出 permission/transition/exit 分布，但尚未做 **A8 vs A2 的“决策路径差异/仓位差异”逐模型对比报告** |
| **牛散效用三维仪表盘** | 已按 Wave/Risk/Capital 拆分，但 **Average Exposure / Time-in-Position 的跨模型对比**（Ablation 内）未输出 |
| **Model Registry** | 已存 settings_blob/python/dependency_hash，但 `code_commit` 为空（环境无 git 输出），且无“registry 变更历史/状态”管理界面 |
| **Legacy→New 验收** | 01951 已 PASS，但 **默认 engine_source 仍未切 canonical**（按验收结论留待你决定） |

需外部条件才能生效

| 建议 | 说明 |
| :-- | :-- |
| **PIT 真实披露表（PIT-A）** | 代码已接线并提供模板，但 `Config/qcfp_disclosure_dates.csv` 未填充真实披露日，系统仍为 PIT-C |
| **PIT Universe 完整记录** | `Config/qcfp_universe.csv` 未配置，正式回测仍提示幸存者偏差风险 |
| **回归/样本外延长验证** | 16 只样本 + PIT-C 下，审查要求的“长期 OOS 证据”需要真实披露日与更大股票池才能成立 |

已按审查要求“冻结不做”的（非缺失）：不加新因子、不设 FLAT→HOLDING 捷径、不优化 Retail Utility 总分、Daily 不升级 Permission——这些是刻意保留的设计约束。

如果你希望，我可以按优先级继续：① Step 级 Trace 落库；② Ablation 机制对比报告（A8 vs A2）；③ 决策卡补全身份字段；④ 单一 G0–G9 研究门。


---

这正是计划要求的诚实状态：代码治理链已全部通过，唯一卡点是没有真实披露日与 PIT 股票池——补上 qcfp_disclosure_dates.csv 与 qcfp_universe.csv 后即可进入 RELEASE。
其他：决策报告新增“第一屏交易者答案”（Institutional/Permission/Setup/Risk/FSM/Position/Target/ACTION + Why 列表）；文档已更新（[2.2 架构设计 V39](C:/Users/Quansheng/Documents/projects/TA_Workflow/Doc/QCFP-MTF 2.2_架构设计.md)、[2.2 使用手册](C:/Users/Quansheng/Documents/projects/TA_Workflow/Doc/QCFP-MTF 2.2_使用手册.md)）。
按计划暂缓（未实施，属刻意纪律）：不加新因子/指标、不调 Q/C/F/P 权重、不为 Sharpe 调参、Live Shadow 需连续运行积累跟踪样本后再与回测预期比较。


---

未执行（附原因）
建议	未执行原因
PIT-A 真实披露日数据本身	代码已接线并提供模板，但需要真实披露日历数据（外部数据源），我无法生成；补齐后 PIT 门自动转 A/B
ADV/参与率约束写入回测主链路	已建流动性模型（max_order_size/fill_ratio）与三情景，但未把 fill_ratio 实际裁剪 target 并入 run_backtest 主链——因为这会改变回测口径，需你确认后再作为默认执行假设
Alpha Ablation（Q/QM/QMW/Flow/Full）与严格 OOS 长期验证	需要更长样本与真实 PIT；现有 layer_ablation 已覆盖 Q/QM/QMW/Full，但 Flow 增量与独立 OOS 样本受数据年限限制，未宣称成立
Live Shadow 长期运行 / Paper Execution 跟踪	需要持续每日运行（外部条件），代码已提供 shadow_live.py，待时间积累样本后与回测预期对比
00371 报告中的季度 Bullish 却 EXIT 的“交易者解释”	已加“Structural ≠ Permission ≠ Action”声明与三类结论，但完整叙事化解释（为什么结构看多仍退出）需结合 00371 具体时间线做深度报告，属 P4 内容
FSM Transition Matrix 完整文档化输出	矩阵已实现且有穷举测试，但未输出为独立报告/可视化矩阵表（当前以测试断言形式存在）


---

未执行（附原因）
建议	未执行原因
PIT-A/B + PIT Universe 正式 Research Validation	需要真实披露日历与 PIT 股票池数据（外部数据源）；代码门已就位（PIT-A 自动识别、research_validation 硬门、PIT 不完整即 INVALID）
Wave 生命周期模型（Discovery→Recovery→Expansion→Mature→Failure）	未实现完整生命周期状态机（wave/signal 已提供 direction/strength/recovery/failure 基础，但生命周期模块需更多波段研究支撑，避免拍脑袋定义）
Wave Ablation 在决策层（No Wave vs Wave）	引擎未接入 wave_signal 决策影响——避免在未充分验证前改变已冻结决策链；Feature Gate 已确保未来不误用，DecisionConfig.use_wave 已预留
Capital Allocation by Wave Quality	TQS 已按质量分档门控，但“按 Wave 质量分配资本”的独立模块未做（依赖 Wave 生命周期先落地）
Permission × Wave 二维贡献矩阵专项报告	Ablation 已有 permission/机制分布，但未生成 Permission×Wave 二维贡献图（可后续基于 A14 + Trade Ledger 补）
Live Shadow 长期运行 / Paper Execution 跟踪	需每日持续运行（外部条件），脚本已就绪，待时间积累
逐笔 Trade Replay 可视化（交互图）	Trade Ledger 已建（CSV/JSON/MD），交互式逐笔重放未做
00371“季度 Bullish 仍 EXIT”完整叙事报告	已加“Structural≠Permission≠Action”声明与三类结论，但深度时间线叙事报告未做（P4 内容）
5 类 Invariant 接入外部 CI	测试已建（G1–G8、不变量矩阵、Risk 单调、Feature Gate、Replay Hash），未接外部 CI 流水线（仓库无 CI 配置）


刻意不做（按审查纪律）：不加新因子/指标、不调 C/F/P 与 TQS 权重追 Sharpe、不优化 Retail Utility、不为 00371 单独调参。
验证：全量 65 模块 PASS；01951 Retail Swing Report（[MD](C:/Users/Quansheng/Documents/projects/TA_Workflow/Report/QCFP_MTF/retail_swing/retail_swing_01951_20260826.md)）、[Trade Ledger JSON](C:/Users/Quansheng/Documents/projects/TA_Workflow/Report/QCFP_MTF/retail_swing/retail_swing_01951_20260826.json)；文档已更新（[2.2 架构设计 V41](C:/Users/Quansheng/Documents/projects/TA_Workflow/Doc/QCFP-MTF 2.2_架构设计.md)）。
下一步若继续，优先级建议：① Wave 生命周期模型 + Capital Allocation（需先补波段研究证据）；② 真实披露日与 PIT Universe 数据就绪后重跑 Release Gate 冲 RELEASE；③ Permission×Wave 二维贡献矩阵与逐笔 Trade Replay 可视化。


---
需要哪些波段研究证据 、真实披露日与 PIT Universe 数据   ？
当前状态：qcfp_universe.csv 与 qcfp_disclosure_dates.csv 都不存在（只有模板）；数据库有 15–16 只股票、季度数据 2004–2026（960 行）、周线 2010–2026。以下是你问的三类数据的具体需求。
一、波段研究证据（Wave research evidence）
这是“要不要给 Wave 层建生命周期/分配资本”的判据，需要四类用现有数据就能产出的证据（不需要新数据源，需要的是分析跑出来）：
证据	具体内容	验收标准
① 信号特征有效性	WaveSignal（as-of：direction/strength/age/acceleration/recovery/failure）→ 前向 1/4/8/12 周收益、MFE/MAE、捕获率	strength/acceleration 分档后前向收益单调且 OOS 复现（否则不得进决策层）
② 生命周期分阶段统计	Discovery/Recovery/Expansion/Mature/Failure 每阶段的 N、前向收益、胜率、MAE、失败率	阶段标签可区分风险（如 Failure 阶段 MAE 显著更大）；阶段转移矩阵稳定
③ 交易质量证据	Trade Ledger 已产出：每笔的 MFE/MAE/持有周/whipsaw/加仓时机；需要补分 TQS 档的 OOS 收益/MDD 单调性	高 TQS 档 OOS 收益 ≥ 低档（否则 TQS 权重无依据）
④ 错失机会证据	已被 BLOCK/Cooldown/Risk 过滤的波段：数量、平均涨幅、≥50%/≥100% 分布（机会成本）	过滤后 False Entry 下降的幅度 > 错失机会的代价（IFV 为正）


最小样本要求：当前 15–16 只、2021 起有效资金流样本太薄（≤5 年 × 16 只 ≈ 几百个决策），分档后每档 N 常 < 30，统计不显著。建议至少 30–50 只 × 8–10 年，波段（≥50%/26 周）样本量达到 500+ 才有分阶段/分档的统计力。
二、真实披露日（Real disclosure dates）
文件：Config/qcfp_disclosure_dates.csv（模板已给），三列：
stock_code,period_end,available_date
00700,2025-12-31,2026-03-20
需要覆盖的规模：每只股票 × 每个季度 = 15–16 只 × 约 20 个季度（2021–2026 有效样本期）≈ 300–350 行；若要完整 2004 起，约 1000+ 行。来源：HKEX 公告/公司财报披露日（akshare/Futu 的公告接口可抓，或人工维护）。影响：存在该文件且逐条匹配 → disclosure_mode=REAL（PIT-A）→ 解锁 research_validation；否则 PIT-C 只能探索。
三、PIT Universe 数据
文件：Config/qcfp_universe.csv，三列：
stock_code,valid_from,valid_to
00700,2004-01-01,2026-12-31
需求要点：必须包含已退市/停牌股票（否则幸存者偏差无法消除），每只股票一段或多段“可投资窗口”（valid_from=上市日，valid_to=退市日/数据截止）。规模：约 40–100 只（含已退市）即可显著缓解偏差。来源：Futu/moomoo 历史股票列表、HKEX 上市/退市记录、akshare。影响：backtest_runner --constraints/正式回测用 strict 过滤；缺失记录 → PIT_INVALID（不会退化为 valid forever）；research_validation/production 硬门通过的前提。
需要我做什么
我可以直接给你一个 数据就绪检查器（scripts/check_pit_data.py）：校验两个 CSV 的格式、覆盖缺口（哪些股票×季度缺披露日、哪些信号期不在 Universe 窗口内）、并输出“若补齐后可把 PIT-C 升到 A/B 的覆盖率”。要我现在写这个检查器吗？